import React from 'react';
import cl from './ButtonWallet.module.css'

const ButtonWallet = ({}) => {
    return (
        <button className={cl.button_wallet}>
        </button>
    );
}
export default ButtonWallet